[pytest]
asyncio_mode = auto
testpaths = tests
addopts =
    --strict-markers
    -q

markers =
    integration: mark test as requiring real external services (Postgres, Redis)
    requires_db: mark test as requiring a running database
    slow: mark test as slow-running
